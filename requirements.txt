# Simulation
mujoco>=3.9
numpy>=1.26

# Perception
torch>=2.2
torchvision>=0.17
pillow>=10.0

# Dashboard / API
fastapi>=0.110
uvicorn[standard]>=0.29

# Testing + lint
pytest>=8.0
httpx>=0.27   # FastAPI TestClient transport (dashboard route tests)
ruff>=0.6     # linter + import sorter (make lint / CI)
